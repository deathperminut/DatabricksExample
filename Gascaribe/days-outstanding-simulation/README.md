# Days Outstanding Simulation

## Instructions

1. Set date in `InitialState.sql`, `Transition.sql`, `AverageDebt.sql`.
2. Save the results of each query in a csv with the same name as the query.
3. Run `Model.py`.
